<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arcee Trinity Chat</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #121212; color: white; display: flex; justify-content: center; padding: 20px; }
        .chat-container { width: 100%; max-width: 800px; background: #1e1e1e; border-radius: 12px; overflow: hidden; display: flex; flex-direction: column; height: 90vh; border: 1px solid #333; }
        #chat-box { flex: 1; overflow-y: auto; padding: 20px; display: flex; flex-direction: column; gap: 15px; }
        .message { padding: 12px 16px; border-radius: 8px; max-width: 80%; line-height: 1.5; }
        .user { background: #3b82f6; align-self: flex-end; }
        .ai { background: #333; align-self: flex-start; border: 1px solid #444; }
        .input-area { padding: 20px; background: #252525; display: flex; gap: 10px; border-top: 1px solid #333; }
        input { flex: 1; padding: 12px; border-radius: 6px; border: 1px solid #444; background: #121212; color: white; outline: none; }
        button { padding: 10px 20px; background: #3b82f6; color: white; border: none; border-radius: 6px; cursor: pointer; font-weight: bold; }
        button:hover { background: #2563eb; }
        .status { font-size: 12px; color: #888; margin-bottom: 5px; }
    </style>
</head>
<body>

<div class="chat-container">
    <div id="chat-box">
        <div class="message ai">Hello! I am <strong>Trinity-Large-Preview</strong>. How can I assist your creative writing or agentic tasks today?</div>
    </div>
    <div class="input-area">
        <input type="text" id="user-input" placeholder="Type your prompt here..." onkeypress="if(event.key === 'Enter') sendMessage()">
        <button onclick="sendMessage()">Send</button>
    </div>
</div>

<script>
    // SECURITY NOTE: In a real app, use a backend to hide your API key.
    const API_KEY = "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"; 
    const MODEL = "arcee-ai/trinity-large-preview:free";

    async function sendMessage() {
        const inputField = document.getElementById('user-input');
        const chatBox = document.getElementById('chat-box');
        const userText = inputField.value.trim();

        if (!userText) return;

        // Add user message to UI
        appendMessage('user', userText);
        inputField.value = '';

        try {
            const response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
                method: "POST",
                headers: {
                    "Authorization": `Bearer ${API_KEY}`,
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({
                    "model": MODEL,
                    "messages": [{ "role": "user", "content": userText }]
                })
            });

            const data = await response.json();
            const aiResponse = data.choices[0].message.content;
            appendMessage('ai', aiResponse);
        } catch (error) {
            appendMessage('ai', "Error: Could not connect to the API. Check your key and console.");
            console.error(error);
        }
    }

    function appendMessage(role, text) {
        const chatBox = document.getElementById('chat-box');
        const div = document.createElement('div');
        div.className = `message ${role}`;
        div.innerText = text;
        chatBox.appendChild(div);
        chatBox.scrollTop = chatBox.scrollHeight;
    }
</script>

</body>
</html>
